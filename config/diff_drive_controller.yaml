controller_manager:
  ros_parameters:
    update_rate: 50

    diff_drive_controller:
      type:diff_drive_controller/DiffDriveController 
  

diff_drive_controller:
  ros_parameters:
    left_wheel_names: ["left_wheel_joint"]
    right_wheel_names: ["right_wheel_joint"]
    wheel_seperation: 0.2 
    wheel_radius: 0.15
    base_frame_id: base_link
    odom_frame_id: odom 
    publish_rate: 50
    cmd_vel_timeout: 0.25

